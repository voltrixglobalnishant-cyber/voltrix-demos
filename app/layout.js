export const metadata = {
    title: "Voltrix CX Demos",
    description: "Live AI concierge demos by Voltrix CX",
  };
  
  export default function RootLayout({ children }) {
    return (
      <html lang="en">
        <body>{children}</body>
      </html>
    );
  }